/**
 * Default templates used for messages produced by different rules of Validations
 * They can be modified, deleted or new templates can be added using Valid template
 * API. Note: the following templates are used only if the rule itself do not
 * have any defined template, otherwise rule templates are in higher priority.
 * @placeholder :message {string} the message which is injected into the template
 * @type {{message_success: string, message_error: string, message_info: string}}
 */
var MT_VALID_TEMPLATES = {

}
